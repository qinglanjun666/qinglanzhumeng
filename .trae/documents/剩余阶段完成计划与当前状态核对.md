## 当前完成情况
- 阶段1/2（布局骨架与样式整合）：已完成。Hero/Sections/栅格与响应式样式已加（示例：`university.html:236–312`，Hero/feature/steps 区块；`styles.css:9–21` 基础样式变量与组件）。
- 阶段3（数据联动与交互）：已完成。详情/基础信息/简介/入学方式/MBTI 联动与“想读/在读/标签点赞”可用（`university.html:114–140` 加载流程；`university.html:923–1008` 交互绑定；API 路由 `api/index.php:33–90`）。
- 阶段4（SEO与可访问性）：部分完成。动态 `title/description/keywords/OG/Twitter/Canonical` 与 JSON-LD 已保留/增强（`university.html:518–598`、`601–723`）；新增按钮 `aria-label` 与图片 `alt` 已覆盖主要模块。
- 阶段5（响应式与性能）：部分完成。新增移动断点与轻量 DOM；网络重试沿用（`university.html:1039–1056`）。
- 阶段6（配置化）：部分完成。Header/品牌色/字体与 Footer 可配置（`top-nav.js:22,27,53–80,82–111`；`site-footer.js:6,36–51`）；`data/site_config.json` 已提供基础结构。
- 阶段7（测试与验证）：未完成。缺少系统性测试矩阵与用例。
- 阶段8（上线与回滚）：未完成。未编制上线说明与回滚策略。

## 待完成阶段与实施计划
### 阶段4完善（SEO/可访问性细化）
- 为所有交互元素补齐 `aria-*` 与键盘焦点态；统一按钮角色与语义。
- 细化 JSON-LD：根据基础信息/MBTI 填充 `sameAs`/`department` 完整性；确保 Canonical 指向 `/university/{id}`。
- 检查文本对比度与图片 `alt` 的覆盖率，输出可访问性清单。

### 阶段5完善（响应式与性能）
- 根据 `styles.breakpoints` 扩展断点至平板（`≥768px && <1024px`）与桌面；调优 Hero/FeatureGrid/CardList 在平板上的列数。
- 性能：懒加载/资源体积核查；避免重复请求；长列表 diff 渲染优化。

### 阶段6扩展（页面级配置化）
- 在 `university.html` 新增配置解析器：
  - 读取 `data/site_config.json` 的 `layout.hero` 与 `sections[]`，将 `textImage/featureGrid/cardList/steps` 映射到现有组件渲染函数。
  - 合并运行时数据（大学详情/基础信息/MBTI/入学方式）与配置静态文案，缺失时采用数据/占位回退。
  - 将 CTA 链接与图标路径从配置注入；样式变量以 `styles.brandColor/font/breakpoints` 生效。
- 扩展配置示例：在 `site_config.json` 增加与需求一致的字段（与用户 YAML/JSON 规格一一对应）。

### 阶段7（测试与验证）
- 编制测试用例：
  - 不同 `id` 的详情页加载；404/400 兜底；“想读/在读/标签点赞”的幂等。
  - 配置切换：品牌色/字体/导航项/各 Section 文案与组合是否正确渲染。
  - 移动/平板/桌面断点与可访问性（键盘导航、屏幕阅读器）。
- 形成问题清单并修复。

### 阶段8（上线与回滚）
- 上线说明：改动范围、配置入口、SEO 检查清单。
- 回滚策略：保留原模板与配置开关；出现问题时快速回退到默认布局。

如确认上述计划，我将按顺序推进阶段4→5→6→7→8，并在实现阶段6时给出配置字段与解析代码的具体变更点。